import { State } from "../models/State";

export class Popup {
    private state: State;

    constructor(state: State) {
        this.state = state;
    };

    public async init(): Promise<void> {
        try {
        } catch (err) {
        };
    };
}
